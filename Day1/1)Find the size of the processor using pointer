#include<stdio.h>
int main(){
int*ptr;
int num=1;
ptr=%num;
printf("Address of num:%p\n",ptr);
ptr++;
printf("address after incrementing ptr:%p\n",ptr);
int size=(ptr-&num)*size of(int);
printf("size of the processors word:%bytes \n",size);
return 0;
}
